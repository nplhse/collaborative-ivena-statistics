<span class="badge bg-{{ row.reviewStatus.value == 'needs_review' ? 'warning' : (row.reviewStatus.value == 'matched' ? 'success' : 'secondary') }}-lt">
    {{ ('indication.review.status.' ~ row.reviewStatus.value)|trans({}, 'allocation') }}
</span>
