{% extends '@Shared/_layout_vertical.html.twig' %}

{% block title %}Reports: {{ reportsPage.headerTitleKey|trans }} – {{ parent() }}{% endblock %}

{% block header %}
    {% embed '@Shared/_embeds/header.html.twig' %}
        {% block header_after %}
            {{ include(('@Shared/_includes/subnav_stats.html.twig')) }}
        {% endblock %}
    {% endembed %}
{% endblock %}

{% block page_header %}
    <div class="container-xl">
        <div class="d-flex align-items-start justify-content-between gap-3">
            <twig:Breadcrumbs :items="[
                { label: 'link.statistics', path: path('app_stats_dashboard') },
                { label: 'link.stats.reports' }
            ]" />

            <div class="flex-shrink-0 d-print-none mt-2">
                {{ include('@Statistics/_data_quality_indicator.html.twig') }}
            </div>
        </div>

        <div class="page-header d-print-none">
            <div class="row g-2 align-items-center">
                <div class="col">
                    <div class="page-pretitle" data-testid="stats-heading-subtitle">
                        {{ reportsPage.headerSubtitleKey|trans }}
                    </div>
                    <h2 class="page-title" data-testid="stats-heading-title">
                        {{ reportsPage.headerTitleKey|trans }}
                    </h2>
                </div>
                {{ include('@Statistics/_header_controls.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

{% block page_content %}
    {{ include('@Statistics/_data_quality_drawer.html.twig') }}

    <div class="row g-4 align-items-start">
        <aside class="col-12 col-lg-4 col-xl-3">
            <div class="position-lg-sticky" style="top: 1rem;">
                {{ include('@Statistics/_explorer_sidebar.html.twig') }}
            </div>
        </aside>
        <div class="col-12 col-lg-8 col-xl-9 min-w-0">
            <div data-testid="stats-reports-widget">
                {{ include('@Statistics/dashboard/_widget_renderer.html.twig', {widget: reportsPage.reportWidget}) }}
            </div>

            {% if overviewPeriodViewModel is defined %}
                {{ include('@Statistics/dashboard/_overview_period_navigation.html.twig') }}
            {% endif %}
        </div>
    </div>
{% endblock %}
