<div class="card mb-3" data-testid="stats-benchmark-selection-card">
    <div class="card-body">
        <div class="row g-4 align-items-start">
            <div class="col-md-6">
                {{ include('@Statistics/benchmarking/_filter_column.html.twig', {
                    columnTitleKey: 'stats.benchmark.primary.label',
                    pageViewModel: selection.primaryPageViewModel,
                    overviewPeriodViewModel: selection.primaryPeriodViewModel,
                    testIdPrefix: 'stats-benchmark-primary',
                    caseCount: report.header.primaryTotal,
                    badgeClass: 'bg-primary-lt',
                }) }}
            </div>
            <div class="col-md-6">
                {{ include('@Statistics/benchmarking/_filter_column.html.twig', {
                    columnTitleKey: 'stats.benchmark.comparison.label',
                    pageViewModel: selection.comparisonPageViewModel,
                    overviewPeriodViewModel: selection.comparisonPeriodViewModel,
                    testIdPrefix: 'stats-benchmark-comparison',
                    caseCount: report.header.comparisonTotal,
                    badgeClass: 'bg-secondary-lt',
                }) }}
            </div>
        </div>
    </div>
</div>
