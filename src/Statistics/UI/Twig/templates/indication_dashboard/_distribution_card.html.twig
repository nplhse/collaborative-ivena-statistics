<twig:Card
    title="{{ titleKey|trans({}, 'statistics') }}"
    headerClass="py-2"
    data-testid="{{ testId }}"
>
    {{ include('@Statistics/indication_dashboard/_distribution_progress.html.twig', {
        rows: rows,
        labelClass: labelClass|default('small'),
    }) }}
</twig:Card>
