<turbo-frame id="{{ frameId }}">
    {{ include('@Statistics/closed_department_assignments/_named_share_list.html.twig', {
        rows: rows,
        titleKey: titleKey,
        testId: testId,
        topListUrl: topListUrl,
    }) }}
</turbo-frame>
