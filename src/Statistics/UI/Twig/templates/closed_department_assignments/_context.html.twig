<div class="text-muted small" data-testid="stats-closed-department-inline-compare-hint">
    {{ 'stats.closed_department.inline_compare_hint'|trans({}, 'statistics') }}
</div>

<twig:Card
    title="{{ 'stats.indication.summary.gender_title'|trans({}, 'statistics') }}"
    headerClass="py-2"
    data-testid="stats-closed-department-gender"
>
    {{ include('@Statistics/closed_department_assignments/_feature_progress.html.twig', {
        rows: dashboard.gender,
        showSwatch: true,
    }) }}
</twig:Card>

<twig:Card
    title="{{ 'stats.closed_department.section.urgency'|trans({}, 'statistics') }}"
    headerClass="py-2"
    data-testid="stats-closed-department-urgency"
>
    {{ include('@Statistics/closed_department_assignments/_feature_progress.html.twig', {
        rows: dashboard.urgency,
        showSwatch: true,
    }) }}
</twig:Card>

<twig:Card
    title="{{ 'stats.closed_department.section.resources'|trans({}, 'statistics') }}"
    headerClass="py-2"
    data-testid="stats-closed-department-resources"
>
    {{ include('@Statistics/closed_department_assignments/_feature_progress.html.twig', {rows: dashboard.resources}) }}
</twig:Card>

<twig:Card
    title="{{ 'stats.closed_department.section.clinical'|trans({}, 'statistics') }}"
    headerClass="py-2"
    data-testid="stats-closed-department-clinical"
>
    {{ include('@Statistics/closed_department_assignments/_feature_progress.html.twig', {rows: dashboard.clinicalFeatures}) }}
</twig:Card>
