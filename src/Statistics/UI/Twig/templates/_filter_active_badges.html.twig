{% if statsFilterDrawer.activeCount|default(0) > 0 and statsFilterDrawer.badges|default([]) is not empty %}
    <twig:ActiveFilters
        :badges="statsFilterDrawer.badges"
        data-testid="statistics-filters-active"
    />
{% endif %}
