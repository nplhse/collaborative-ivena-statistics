{{ include('@Statistics/geographic_map/_map.html.twig', {
    payload: heatmap.mapPayload,
    testId: 'stats-isochrone-origin-map',
    geoUrl: null,
    showValueModes: false,
    showExpand: true,
    showIsochroneLegend: true,
    frameClass: 'isochrone-origin-map-frame',
    unknownCount: heatmap.unknownCount,
    beyondMaxCount: heatmap.beyondMaxCount,
    titleKey: 'stats.isochrone_origin_map.title',
}) }}
