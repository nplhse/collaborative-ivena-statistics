{% extends '@Shared/_layout_vertical.html.twig' %}

{% block title %}{{ 'stats.case_flow.title'|trans({}, 'statistics') }} – {{ parent() }}{% endblock %}

{% block header %}
    {% embed '@Shared/_embeds/header.html.twig' %}
        {% block header_after %}
            {{ include('@Shared/_includes/subnav_stats.html.twig') }}
        {% endblock %}
    {% endembed %}
{% endblock %}

{% block page_header %}
    <twig:PageHeader
        :pretitle="'stats.case_flow.pretitle'|trans({}, 'statistics')"
        :title="'stats.case_flow.title'|trans({}, 'statistics')"
        pretitleTestId="stats-case-flow-heading-subtitle"
        titleTestId="stats-case-flow-heading-title"
    >
        <twig:block name="breadcrumbs">
            <twig:Breadcrumbs :items="[
                { label: 'link.statistics', path: path('app_stats_dashboard') },
                { label: 'stats.case_flow.title' }
            ]" />
        </twig:block>
        <twig:block name="meta">
            {{ include('@Statistics/_data_quality_indicator.html.twig') }}
        </twig:block>
        <twig:block name="actions">
            {{ include('@Statistics/_header_actions.html.twig') }}
        </twig:block>
    </twig:PageHeader>
{% endblock %}

{% block page_content %}
    {{ include('@Statistics/_data_quality_drawer.html.twig') }}
    {{ include('@Statistics/_filter_drawer.html.twig') }}
    {{ include('@Statistics/_filter_active_badges.html.twig') }}

    <div data-controller="case-flow-charts"
         data-case-flow-charts-payload-value="{{ chartPayload|json_encode|e('html_attr') }}">

        {{ include('@Statistics/case_flow/_summary_deck.html.twig') }}

        <div class="row g-3 align-items-start mt-1">
            <div class="col-lg-8 d-flex flex-column gap-3">
                {{ include('@Statistics/case_flow/_map_card.html.twig') }}
                {{ include('@Statistics/case_flow/_segment_profile_card.html.twig') }}
            </div>

            <div class="col-lg-4 d-flex flex-column gap-3">
                {{ include('@Statistics/case_flow/_insight_highlights.html.twig') }}
                {{ include('@Statistics/case_flow/_dispatch_flow_card.html.twig') }}

                {% if dashboard.mode.value != 'system_flow' %}
                    <div class="card" data-testid="stats-case-flow-origin-bar">
                        <div class="card-header py-2">
                            <h3 class="card-title mb-0">{{ 'stats.case_flow.section.origin_distribution'|trans({}, 'statistics') }}</h3>
                        </div>
                        <div class="card-body">
                            <div class="chart-sm" data-case-flow-charts-target="originBarChart"></div>
                        </div>
                    </div>
                {% endif %}

                {% if dashboard.mode.value == 'system_flow' %}
                    {% if dashboard.destinationTierCard %}
                        {{ include('@Statistics/case_flow/_structure_distribution_card.html.twig', {
                            card: dashboard.destinationTierCard,
                            testId: 'stats-case-flow-tier-structure',
                        }) }}
                    {% endif %}
                    {% if dashboard.destinationLocationCard %}
                        {{ include('@Statistics/case_flow/_structure_distribution_card.html.twig', {
                            card: dashboard.destinationLocationCard,
                            testId: 'stats-case-flow-location-structure',
                        }) }}
                    {% endif %}
                    {% if dashboard.destinationSizeCard %}
                        {{ include('@Statistics/case_flow/_structure_distribution_card.html.twig', {
                            card: dashboard.destinationSizeCard,
                            testId: 'stats-case-flow-size-structure',
                        }) }}
                    {% endif %}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
