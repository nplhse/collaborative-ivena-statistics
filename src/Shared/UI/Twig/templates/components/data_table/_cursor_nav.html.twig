<ul class="pagination m-0 ms-auto">
    {% set previousUrl = this.cursorPreviousUrl %}
    {% if previousUrl %}
        <li class="page-item">
            <a class="page-link" href="{{ previousUrl }}">
                <twig:ux:icon name="tabler:chevron-left" width="1em" height="1em" />
                {{ 'label.previous'|trans({}, 'messages') }}
            </a>
        </li>
    {% else %}
        <li class="page-item disabled">
            <a class="page-link" href="#" tabindex="-1" aria-disabled="true">
                <twig:ux:icon name="tabler:chevron-left" width="1em" height="1em" />
                {{ 'label.previous'|trans({}, 'messages') }}
            </a>
        </li>
    {% endif %}

    {% set nextUrl = this.cursorNextUrl %}
    {% if nextUrl %}
        <li class="page-item">
            <a class="page-link" href="{{ nextUrl }}">
                {{ 'label.next'|trans({}, 'messages') }}
                <twig:ux:icon name="tabler:chevron-right" width="1em" height="1em" />
            </a>
        </li>
    {% else %}
        <li class="page-item disabled">
            <a class="page-link" href="#" tabindex="-1" aria-disabled="true">
                {{ 'label.next'|trans({}, 'messages') }}
                <twig:ux:icon name="tabler:chevron-right" width="1em" height="1em" />
            </a>
        </li>
    {% endif %}
</ul>
