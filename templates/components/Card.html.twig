<div class="{{ this.cssClass }}">
    {% if title %}
        <div class="card-header">
            <h3 class="card-title">{{ title }}</h3>
        </div>
    {% endif %}
    <div class="card-body">
        {% block content %}{% endblock %}
    </div>
</div>
